spring:
  mvc:
    format:
      date-time: yyyy-MM-dd HH:mm:ss

eureka:
  instance:
    metadata-map:
      zone: ru-1
  client:
    service-url:
      defaultZone: http://localhost:8762/eureka

ribbon:
  eureka:
    enabled: true